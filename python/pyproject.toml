[project]
name = "big-oh-no"
version = "0.1.0"
description = "A collection of utterly useless yet entertaining sorting algorithms"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.1",
    "rich>=14.3.3",
]

[project.scripts]
big-oh-no = "cli:main"
wait-sort = "wait_sort:main"
stalin-sort = "stalin_sort:main"
linus-sort = "linus_sort:main"
